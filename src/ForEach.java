import java.io.*;

class ForEach {
    public static void main(String[] args)
    {
        String names[] = { "Sweta", "Gudly", "Amiya" }; // We can Write like  String[] names 

        for (String name : names) {
            System.out.println("Name: " + name);
        }
        
		/*
		 * ForEach a= new ForEach(); System.out.print(a.hashCode());
		 */
    }
    
    
}